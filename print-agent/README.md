# Sawyun Print Agent

Build: cargo build --release
Run: target\release\sawyun-print-agent.exe

The agent listens only on 127.0.0.1:17891. Local Vite origins work automatically.
For production set SAWYUN_PRINT_ORIGINS to the exact web origin, for example https://app.sawyunpos.com.
